# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/test-results
/playwright-report
/blob-report
/playwright/.cache

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# local-only design/roadmap docs
/docs

# analytics: raw MCP snapshots / SQLite DB / 生成レポート は GitHub に上げない
# schema.sql / queries / SKILL.md は commit する
/analytics/data
/analytics/snapshots
/analytics/reports

# python: analytics/scripts を実行すると生成される。中身はバイトコードなので追跡しない
__pycache__/
*.py[cod]

# MCP: 絶対パス + サービスアカウント参照を含むためローカル専用
/.mcp.json

# typescript
*.tsbuildinfo
next-env.d.ts
