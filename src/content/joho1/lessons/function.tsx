import Link from "next/link";
import { LessonPlayground } from "@/components/joho1/LessonPlayground";

export default function FunctionLesson() {
  return (
    <>
      <h2>覚える関数の一覧は存在しない</h2>
      <p>
        他のプログラミング言語を勉強したことがあると、
        「この言語で使える関数の一覧」を覚えようとしてしまいがちです。
        共通テストのプログラム表記では、その勉強のしかたは意味がありません。
      </p>
      <p>
        <strong>どの問題でも使える決まった関数は用意されていません。</strong>
        <code>要素数()</code> も <code>最大値()</code> も、その問題の
        <strong>【関数の説明】</strong>として日本語で意味が示され、
        そこで初めて使えるようになります。
      </p>

      <h2>実際の書かれ方</h2>
      <p>
        令和 8 年度の本試験では、プログラムの前にこう書かれていました。
      </p>
      <blockquote>
        <p>
          要素数(配列) … 引数として配列が与えられ、その配列の要素数を返す。
          例えば、配列 X が [6, 9, 1] であるとき、要素数(X) は 3 となる。
        </p>
      </blockquote>
      <p>
        <strong>必ず例がついています。</strong>
        意味が読み取れなかったら、説明の文章より先に例を見てください。
        「何を渡すと何が返るか」が 1 行で分かります。
      </p>

      <h2>使うときの形</h2>
      <p>
        関数名のうしろにカッコを書き、渡すものをカンマで区切って並べます。
        返ってきた値は、そのまま変数に入れたり式の中で使ったりできます。
      </p>

      <LessonPlayground
        indexBase={1}
        code={`Touchaku = [0, 3, 4, 10, 11, 12]
kyakusu = 要素数(Touchaku)
表示する("人数は", kyakusu)
表示する("大きいほうは", 最大値(3, 7))
`}
      />

      <h2>自分で関数を定義する書き方は出ていない</h2>
      <p>
        試作問題から令和 8 年度の追試験まで、
        <strong>受験者が自分で関数を定義する構文は一度も出ていません。</strong>
        関数はいつも「問題文で与えられて、呼び出すだけ」です。
      </p>
      <p>
        したがって対策としてやるべきことは、書き方を覚えることではなく、
        <strong>初めて見る関数の説明をその場で読み取って使うこと</strong>です。
        これは知識ではなく読解の練習なので、
        <Link href="/joho1">実行シミュレーター</Link>{" "}
        に問題のプログラムを貼って、関数の返す値を実際に確かめるのがいちばん早く身につきます。
      </p>
    </>
  );
}
