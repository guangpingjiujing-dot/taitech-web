import Link from "next/link";
import { Playground } from "@/components/fe/Playground";
import { findFeLesson } from "@/content/fe/lessons";

const lesson = findFeLesson("function")!;

const procCode = `○挨拶(文字列型: 名前)
  print("こんにちは")
  print(名前)

挨拶("太郎")
挨拶("花子")
`;

const factorialCode = `○整数型: 階乗(整数型: n)
  if (n ≦ 1)
    return 1
  endif
  return n * 階乗(n - 1)

print(階乗(5))
`;

export const faq = [
  {
    q: "関数と手続きは何が違いますか？",
    a: "戻り値を返すのが関数、返さないのが手続きです。関数は「○戻り値型: 名前(...)」、手続きは「○名前(...)」と書きます。呼び出す側から見ると、関数は式として値を受け取れる、手続きは実行するだけ、という違いになります。",
  },
  {
    q: "return を書かないとどうなりますか？",
    a: "関数 (戻り値を持つもの) で return を書かずに終わると、呼び出し元は「未定義の値」を受け取ります。手続きの場合は最後まで実行して自然に呼び出し元に戻ります。関数を定義するときは return を書き忘れないよう注意しましょう。",
  },
  {
    q: "再帰呼び出しは書けますか？",
    a: "書けます。関数の中で自分自身を呼び出すことができ、階乗やフィボナッチのような教科書的な例は問題なく動きます。無限再帰になると 100000 ステップ超で自動停止するため、ブラウザが固まる心配はありません。",
  },
];

export default function FunctionBody() {
  return (
    <>
      <h2>関数 は ○ から始めて宣言する</h2>
      <p>
        処理をひとまとまりにして名前を付けたものが関数です。
        基本情報の擬似言語では、関数定義の先頭に
        <strong>○</strong> という記号を置きます。
      </p>
      <pre>
        <code>{`○整数型: 最大値(整数型: a, 整数型: b)
  if (a > b)
    return a
  endif
  return b`}</code>
      </pre>
      <p>
        <code>○整数型:</code> の部分が「戻り値の型」、
        <code>最大値</code> が関数名、
        <code>(整数型: a, 整数型: b)</code>{" "}
        が引数リストです。引数もそれぞれに型を書きます。
        <code>return</code> で呼び出し元に値を返し、そこで関数は終わります。
      </p>

      <h2>ブラウザで動かしてみる</h2>
      <p>
        最大値を返す関数を 2 回呼び出す例です。
        <strong>一行ずつ実行</strong>{" "}
        を押していくと、関数呼び出しに入った瞬間に
        引数 <code>a</code>, <code>b</code>{" "}
        が右側の変数テーブルに現れ、return
        で呼び出し元に戻ると消えていくのが目で追えます。
      </p>
      <div className="not-prose my-6">
        <Playground initialCode={lesson.sampleCode} />
      </div>
      <p>
        出力は <code>7</code> と <code>10</code> です。
        引数の値を変えて再実行すると、同じ関数が違う入力で動く様子を確認できます。
      </p>

      <h2>戻り値のない「手続き」は ○名前(...) で書く</h2>
      <p>
        値を返さず、何かを実行するだけの処理は「手続き」と呼びます。
        手続きの定義では、戻り値の型を書きません。
      </p>
      <div className="not-prose my-6">
        <Playground initialCode={procCode} />
      </div>
      <p>
        <code>挨拶</code> は戻り値を持たないので、
        <code>整数型: 結果 ← 挨拶(&quot;太郎&quot;)</code>{" "}
        のように受け取ることは想定されていません。
        「実行するだけ」の処理は手続き、「値を計算して返す」処理は関数、
        と切り分けて考えると読み書きが楽になります。
      </p>

      <h2>関数の呼び出しは式の中でも使える</h2>
      <p>
        戻り値を持つ関数は、式の途中に埋め込んで使えます。
      </p>
      <pre>
        <code>{`整数型: x ← 最大値(3, 7)              // 変数に代入
整数型: y ← 最大値(3, 7) + 最大値(2, 5) // 式の中で複数回呼ぶ
if (最大値(a, b) > 10)                // 条件式の中で呼ぶ
  print("大きい")
endif`}</code>
      </pre>
      <p>
        関数呼び出しは 1 つの値のように振る舞うため、
        変数を書ける場所ならほぼどこでも使えます。
      </p>

      <h2>再帰: 自分自身を呼び出す</h2>
      <p>
        関数の中で自分自身を呼び出すことを再帰と呼びます。
        階乗 <code>n! = n × (n-1)!</code>{" "}
        のように「自分より 1 つ小さい問題」で表せる処理と相性が良い書き方です。
      </p>
      <div className="not-prose my-6">
        <Playground initialCode={factorialCode} />
      </div>
      <p>
        出力は <code>120</code> (= 5! = 5×4×3×2×1) です。
        <strong>一行ずつ実行</strong> を押していくと、
        <code>階乗(5)</code> の中で <code>階乗(4)</code> が呼ばれ、
        さらにその中で <code>階乗(3)</code> が呼ばれ… と、
        変数テーブルにいくつもの <code>n</code>{" "}
        が (階乗) というラベル付きで積み上がっていく様子が観察できます。
        必ず「これ以上再帰しない終了条件」(ここでは
        <code>n ≦ 1</code>) を書くのが再帰のコツです。
      </p>

      <h2>試験でつまずきやすいポイント</h2>
      <ul>
        <li>
          <strong>○ を書き忘れる</strong>:
          関数定義の先頭の ○ を忘れると、
          その行はただの変数宣言や式として扱われて意味が変わる
        </li>
        <li>
          <strong>return 忘れ</strong>:
          関数で return を書き忘れると呼び出し元が未定義の値を受け取る
        </li>
        <li>
          <strong>引数の型と実引数の型がずれる</strong>: 整数型を要求する関数に
          文字列を渡すと実行時エラー
        </li>
        <li>
          <strong>再帰の終了条件</strong>:
          終了条件を書き忘れると無限再帰になる。
          「引数がどう変わっていくか」を追って、
          必ず基底ケースに到達することを確認する
        </li>
      </ul>

      <h2>6 本のレッスンをすべて読み終えたら</h2>
      <p>
        変数・条件分岐・while・for・配列・関数と、
        基本情報の擬似言語で使う主要な構文を一通り学びました。
        次のステップはこの 3 つがおすすめです。
      </p>
      <ul>
        <li>
          <Link href="/fe">実行シミュレーター</Link>{" "}
          に戻り、自分でコードを書いて動かしてみる
        </li>
        <li>
          <Link href="/fe/transpile">多言語横並び比較ツール</Link>{" "}
          で、擬似言語と Python / TypeScript を並べて読み比べる
        </li>
        <li>
          過去問集を 1 冊解いて、実際の科目 B の設問形式に慣れる
        </li>
      </ul>
    </>
  );
}
